export interface signup {
  id: number;
  userid: string;
  username: string;
  email: string;
}

export interface userName {
  username: string;
}

export interface id {
  id: string;
}
export interface email {
  username: string;
  email: string;
}
