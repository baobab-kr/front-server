import styled from "styled-components";

export const TopButton = styled.button`
  position: fixed;
  bottom: 40px;
  right: 40px;
  width: 50px;
  height: 50px;
  border-radius: 25px;
  border: none;
  background: #8fceb1;
  color: white;

  &:hover {
    cursor: pointer;
  }
`;
